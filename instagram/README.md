Use `instagram/drafts/images/` for assets that are scheduled or not yet posted.
Use `instagram/live/` for assets that are already live on Instagram.

Root-level `instagram/*.png` files are retained temporarily for backward compatibility with older scheduled URLs.
